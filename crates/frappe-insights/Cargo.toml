[package]
name = "frappe-insights"
version = "0.1.0"
edition = "2024"

[dependencies]
duckdb = { version = "1.10503.1", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
log = "0.4"

[dev-dependencies]
tempfile = "3.10"

