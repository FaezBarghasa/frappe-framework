# Architecture: frappe-framework

This workspace acts as the engine, providing the multi-tenant QUIC network layer (frappe-net), dynamic schema compiler (frappe-meta), and sandboxed Rhai scripting (frappe-framework core).

This project follows the strict typing and thread-safe principles of the overarching Rust ERP architecture.
